name: FWKarate2xx CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

  # 👇 Controla si quieres ejecutar Gatling
  RUN_PERFORMANCE: false

on:

  push:
    branches:
      - master

  pull_request:
    branches:
      - master

permissions:
  contents: read
  pages: write
  id-token: write

jobs:

  # =========================================================
  # FUNCTIONAL TESTS
  # =========================================================

  functional-tests:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Run functional tests
        run: ./gradlew apiTest -Ptags=@smokes

      - name: Upload Karate Reports
        uses: actions/upload-artifact@v4
        with:
          name: karate-reports
          path: build/karate-reports

  # =========================================================
  # PERFORMANCE TESTS
  # =========================================================

  performance-tests:

    needs: functional-tests

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Grant execute permission
        run: chmod +x gradlew

      # 👇 Solo ejecuta Gatling si RUN_PERFORMANCE=true
      - name: Run performance tests
        if: env.RUN_PERFORMANCE == 'true'
        run: ./gradlew performanceTest -Ptags=@perf

      # 👇 Prepara reporte limpio para Pages
      - name: Prepare Gatling Site
        if: env.RUN_PERFORMANCE == 'true'
        run: |

          mkdir -p build/gatling-site

          latest=$(ls -td build/reports/gatling/* | head -1)

          cp -r "$latest"/* build/gatling-site/

      # 👇 Upload report Gatling
      - name: Upload Gatling Reports
        if: env.RUN_PERFORMANCE == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: gatling-report
          path: build/gatling-site

      # 👇 Mensaje bonito si está deshabilitado
      - name: Skip performance tests
        if: env.RUN_PERFORMANCE == 'false'
        run: |
          echo "Performance tests disabled"

  # =========================================================
  # DEPLOY REPORTS
  # =========================================================

  deploy-reports:

    needs:
      - performance-tests

    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:

      # =====================================================
      # Karate Reports
      # =====================================================

      - name: Download Karate Reports
        uses: actions/download-artifact@v4
        with:
          name: karate-reports
          path: site/karate

      # =====================================================
      # Gatling Reports
      # =====================================================

      - name: Download Gatling Reports
        if: env.RUN_PERFORMANCE == 'true'
        uses: actions/download-artifact@v4
        with:
          name: gatling-report
          path: site/gatling

      # =====================================================
      # Upload GitHub Pages
      # =====================================================

      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      # =====================================================
      # Deploy
      # =====================================================

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4